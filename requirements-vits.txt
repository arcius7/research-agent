# OPTIONAL — VITS2 neural voice.
# Without this, "read aloud" still works via the browser's built-in speech.
# Install on its own (these are large downloads):
#
#   pip install -r requirements-vits.txt
#
torch>=2.2.0
torchaudio>=2.2.0
soundfile>=0.12.0
phonemizer>=3.2.0

# Note: torchtext is required by VITS for vocab handling but is deprecated and
# may lack wheels on Python 3.12. If `pip install torchtext` fails, either use
# Python 3.11 for the voice feature, or stick with the browser fallback.
torchtext>=0.15.0
