# LangGraph agent
langgraph>=0.2.0
requests>=2.31.0

# Vector store  (Rust extension — pre-built wheel on PyPI)
turbovec[langchain]

# Document loaders
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0

# TTS / VITS2
torch>=2.0.0
torchaudio>=2.0.0
torchtext>=0.15.0
soundfile>=0.12.0
phonemizer>=3.2.0
PyYAML>=6.0

# Numeric / ML
numpy>=1.24.0
