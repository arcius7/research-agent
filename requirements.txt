# ─────────────────────────────────────────────────────────────────────────────
# Research Agent — single install file.
# Every version is pinned to a known-good set, so pip installs in one pass
# with NO resolver backtracking.
#
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Core: agent graph + RAG + document loading
langgraph==1.2.2
langchain-core==1.4.0
turbovec[langchain]==0.7.0
pypdf==6.12.2
python-docx==1.2.0
openpyxl==3.1.5
numpy==2.4.6
requests==2.34.2
python-dotenv==1.2.2

# MP3 export for the spoken summary (bundled LAME — no ffmpeg/Homebrew needed)
lameenc==1.8.2

# Page Index (optional /api/tree endpoint) moved to requirements-pageindex.txt —
# litellm + pymupdf + PyPDF2 are heavy and only needed for that feature.

# Neural voice (VITS) is NOT here on purpose — torch is huge and torchtext has
# no Python 3.12 wheel. Audio uses macOS `say` / the browser voice instead.
# If you specifically want VITS: see requirements-vits.txt (needs Python 3.11
# and a trained checkpoint).
