# ── CORE — installs fast, makes the app fully usable ─────────────────────────
# Upload · embed · ask (RAG) · references (SearXNG) · web search · browser voice
#
#   pip install -r requirements.txt
#
langgraph>=0.2.0
requests>=2.31.0
turbovec[langchain]
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
numpy>=1.24.0

# ── OPTIONAL add-ons (install only if you want these features) ───────────────
# Page-index tree (PageIndex):     pip install -r requirements-pageindex.txt
# VITS neural voice (else browser): pip install -r requirements-vits.txt
#
# External services (not pip):
#   Ollama       — ollama pull gemma4:e4b ; ollama pull nomic-embed-text
#   SearXNG      — docker compose up -d
#   agentmemory  — npx @agentmemory/agentmemory   (optional)
